---
interface Props {
  locale: string;
}

const { locale } = Astro.props;
const isEs = locale === "es";

// Placeholder data - user can replace with actual content
const attractions = [
  {
    title_es: "Playa Boca del Río",
    title_en: "Boca del Río Beach",
    image:
      "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?w=600&h=400&fit=crop",
    description_es:
      "La playa más popular de Ilo, perfecta para relajarse y disfrutar del sol.",
    description_en:
      "The most popular beach in Ilo, perfect for relaxing and enjoying the sun.",
  },
  {
    title_es: "Puerto de Ilo",
    title_en: "Port of Ilo",
    image:
      "https://images.unsplash.com/photo-1544551763-46a013bb70d5?w=600&h=400&fit=crop",
    description_es: "Uno de los puertos más importantes del sur del Perú.",
    description_en: "One of the most important ports in southern Peru.",
  },
  {
    title_es: "Gastronomía Local",
    title_en: "Local Gastronomy",
    image:
      "https://images.unsplash.com/photo-1504674900247-0877df9cc836?w=600&h=400&fit=crop",
    description_es:
      "Disfruta de los mejores mariscos y platos tradicionales de la costa peruana.",
    description_en:
      "Enjoy the best seafood and traditional dishes from the Peruvian coast.",
  },
];

const hotels = [
  {
    name: "Hotel Leyton",
    rating: 4,
    price: "$60/noche",
    image:
      "https://images.unsplash.com/photo-1566073771259-6a8506099945?w=400&h=300&fit=crop",
    link: "#",
  },
  {
    name: "Gran Hotel Ilo",
    rating: 4,
    price: "$80/noche",
    image:
      "https://images.unsplash.com/photo-1551882547-ff40c63fe5fa?w=400&h=300&fit=crop",
    link: "#",
  },
];

const quickFacts = [
  { icon: "🌡️", label_es: "Clima", label_en: "Weather", value: "20-25°C" },
  {
    icon: "⏰",
    label_es: "Zona horaria",
    label_en: "Timezone",
    value: "UTC-5 (PET)",
  },
  {
    icon: "✈️",
    label_es: "Aeropuerto",
    label_en: "Airport",
    value: "Tacna (TQM)",
  },
  {
    icon: "🚗",
    label_es: "Desde Tacna",
    label_en: "From Tacna",
    value: "~2h by car",
  },
];
import Wrapper from "./Wrapper.astro";
---

<section id="travel" class="py-24 bg-card-bg relative overflow-hidden">
  <!-- Decorative Elements -->
  <div
    class="absolute top-0 right-0 w-[400px] h-[400px] bg-primary-500/5 rounded-full blur-[100px]"
  >
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-16">
      <span
        class="inline-block px-4 py-1 bg-primary-500/10 text-primary-400 rounded-full text-sm font-medium mb-4 border border-primary-500/20"
      >
        {isEs ? "Descubre Ilo" : "Discover Ilo"}
      </span>
      <h2
        class="text-3xl sm:text-4xl md:text-5xl font-black text-white mb-4 font-heading"
      >
        {isEs ? "Tu Viaje a " : "Your Trip to "}<span class="text-primary-400"
          >Ilo</span
        >
      </h2>
      <p class="text-gray-400 text-lg max-w-2xl mx-auto">
        {
          isEs
            ? "Conoce nuestro hermoso puerto en la costa sur del Perú y planifica tu estadía."
            : "Discover our beautiful port city on the southern coast of Peru and plan your stay."
        }
      </p>
    </div>

    <!-- Quick Facts -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-16">
      {
        quickFacts.map((fact) => (
          <div class="p-4 bg-dark-bg rounded-2xl border border-card-border text-center">
            <span class="text-2xl mb-2 block">{fact.icon}</span>
            <p class="text-gray-500 text-xs uppercase tracking-wider mb-1">
              {isEs ? fact.label_es : fact.label_en}
            </p>
            <p class="text-white font-bold">{fact.value}</p>
          </div>
        ))
      }
    </div>

    <!-- Attractions Bento Grid -->
    <div class="mb-16">
      <h3 class="text-2xl font-bold text-white mb-8 font-heading">
        {isEs ? "Qué visitar" : "What to Visit"}
      </h3>
      <div class="grid md:grid-cols-3 gap-6">
        {
          attractions.map((item, idx) => (
            <div
              class={`group relative overflow-hidden rounded-2xl ${idx === 0 ? "md:col-span-2 md:row-span-2" : ""}`}
            >
              <img
                src={item.image}
                alt={isEs ? item.title_es : item.title_en}
                class={`w-full object-cover transition-transform duration-500 group-hover:scale-110 ${idx === 0 ? "h-full min-h-[300px] md:min-h-[400px]" : "h-48"}`}
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black via-black/40 to-transparent opacity-80" />
              <div class="absolute bottom-0 left-0 right-0 p-6">
                <h4 class="text-xl font-bold text-white mb-2">
                  {isEs ? item.title_es : item.title_en}
                </h4>
                <p
                  class={`text-gray-300 text-sm ${idx === 0 ? "" : "line-clamp-2"}`}
                >
                  {isEs ? item.description_es : item.description_en}
                </p>
              </div>
            </div>
          ))
        }
      </div>
    </div>

    <!-- Hotels Section -->
    <div>
      <h3 class="text-2xl font-bold text-white mb-8 font-heading">
        {isEs ? "Dónde hospedarte" : "Where to Stay"}
      </h3>
      <div class="grid md:grid-cols-2 gap-6">
        {
          hotels.map((hotel) => (
            <a
              href={hotel.link}
              class="group flex flex-col sm:flex-row gap-4 p-4 bg-dark-bg rounded-2xl border border-card-border hover:border-primary-500/30 transition-all"
            >
              <div class="w-full sm:w-40 h-32 flex-shrink-0 rounded-xl overflow-hidden">
                <img
                  src={hotel.image}
                  alt={hotel.name}
                  class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110"
                  loading="lazy"
                />
              </div>
              <div class="flex-grow">
                <h4 class="text-lg font-bold text-white group-hover:text-primary-300 transition-colors">
                  {hotel.name}
                </h4>
                <div class="flex items-center gap-1 my-2">
                  {[...Array(5)].map((_, i) => (
                    <svg
                      class={`w-4 h-4 ${i < hotel.rating ? "text-amber-400" : "text-gray-600"}`}
                      fill="currentColor"
                      viewBox="0 0 20 20"
                    >
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                  ))}
                </div>
                <p class="text-primary-400 font-semibold">{hotel.price}</p>
              </div>
              <div class="flex items-center">
                <span class="text-gray-500 group-hover:text-primary-400 transition-colors">
                  <svg
                    class="w-6 h-6"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M9 5l7 7-7 7"
                    />
                  </svg>
                </span>
              </div>
            </a>
          ))
        }
      </div>

      <p class="text-center text-gray-500 text-sm mt-8">
        {
          isEs
            ? "* Los precios son aproximados y pueden variar según la temporada."
            : "* Prices are approximate and may vary by season."
        }
      </p>
    </div>
  </div>
</section>
