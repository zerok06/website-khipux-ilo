---
import heroData from "../data/hero.json";
import siteData from "../data/site.json";

interface Props {
  locale: string;
}

const { locale } = Astro.props;
const content = heroData[locale as keyof typeof heroData];
const isEs = locale === "es";

const backgroundSymbols = [
  "/images/patterns/symbol1.svg",
  "/images/patterns/symbol2.svg",
  "/images/patterns/symbol1.svg",
  "/images/patterns/symbol2.svg",
];
---

<section
  class="relative min-h-screen flex items-center bg-bg overflow-hidden pt-20 lg:pt-0"
>
  <!-- Símbolos culturales Removed -->

  <div class="relative z-10 w-full max-w-[90rem] mx-auto px-6 lg:px-12">
    <div class="grid lg:grid-cols-2 gap-16 lg:gap-24 items-center">
      <!-- Left: Text Content -->
      <div class="space-y-6 animate-slide-in-left">
        <!-- Event Badge -->
        <div class="flex items-center gap-4">
          <div
            class="flex items-center gap-2 text-text-muted text-sm tracking-wider uppercase font-medium"
          >
            <span class="w-6 h-px bg-primary-500"></span>
            <span>{content.event_type}</span>
          </div>
        </div>

        <!-- Title - Slightly Reduced Size for better flow -->
        <div>
          <h1
            class="text-5xl sm:text-6xl lg:text-[5.5rem] font-bold text-text leading-[1.05] tracking-tight font-heading"
          >
            KHIPU<span class="text-primary-500">x</span> Ilo
          </h1>
          <div class="w-24 h-1.5 bg-primary-500 mt-5 rounded-full"></div>
        </div>

        <!-- Subtitle -->
        <p class="text-lg sm:text-xl text-gray-300 max-w-lg leading-relaxed">
          {content.description}
        </p>

        <!-- Event Info -->
        <div class="flex flex-wrap gap-x-8 gap-y-3 text-base font-medium">
          <div class="flex items-center gap-2 text-gray-400">
            <svg
              class="w-5 h-5 text-primary-500"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
              ></path>
            </svg>
            <span>{isEs ? "18-19 Agosto, 2026" : "Aug 18-19, 2026"}</span>
          </div>
          <div class="flex items-center gap-2 text-gray-400">
            <svg
              class="w-5 h-5 text-primary-500"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
              ></path>
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
            <span>Ilo, Perú</span>
          </div>
        </div>

        <!-- CTA -->
        <div class="flex flex-wrap gap-4 pt-4">
          <a
            href={isEs ? "/es/register" : "/en/register"}
            class="group inline-flex items-center gap-2 px-8 py-3.5 bg-primary-500 hover:bg-primary-600 text-white rounded-xl font-bold text-lg transition-all duration-300 shadow-lg shadow-primary-500/25 hover:shadow-primary-500/40 hover:-translate-y-0.5"
          >
            <span>{content.cta_primary}</span>
            <svg
              class="w-5 h-5 transition-transform group-hover:translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2.5"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
          <a
            href={isEs ? "/es/schedule" : "/en/schedule"}
            class="inline-flex items-center gap-2 px-8 py-3.5 border border-gray-700 text-gray-300 hover:text-white hover:border-gray-500 rounded-xl font-medium text-lg transition-all hover:bg-white/5"
          >
            {content.cta_secondary}
          </a>
        </div>

        <!-- Stats Row (Moved closer) -->
        <div class="pt-8 border-t border-gray-800/60 max-w-lg">
          <div class="grid grid-cols-3 gap-8">
            {
              content.stats.map(
                (stat: { value: string; label: string }, index: number) => (
                  <div
                    class="animate-fade-in-up"
                    style={`animation-delay: ${index * 0.1}s`}
                  >
                    <div class="text-3xl sm:text-4xl font-bold text-primary-500 font-heading">
                      {stat.value}
                    </div>
                    <div class="text-xs text-gray-500 uppercase tracking-widest mt-1 font-semibold">
                      {stat.label}
                    </div>
                  </div>
                ),
              )
            }
          </div>
        </div>
      </div>

      <!-- Right: Chiribaya Image -->
      <div
        class="relative hidden lg:flex items-center justify-center lg:justify-end"
      >
        <div class="relative w-full max-w-[500px]">
          <!-- Glow - Made softer -->
          <div class="absolute inset-0 flex items-center justify-center">
            <div
              class="w-[120%] h-[120%] rounded-full bg-primary-500/10 blur-[100px] animate-pulse-slow"
            >
            </div>
          </div>

          <!-- Image with Breeze effect -->
          <div class="relative z-10">
            <img
              src="/chiribaya.webp"
              alt="Chiribaya Cultural Figure"
              class="w-full h-auto chiribaya-breeze"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Soft Breeze & Lighting Animation */
  .chiribaya-breeze {
    animation:
      breeze-float 8s ease-in-out infinite,
      soft-light 5s ease-in-out infinite alternate;
    filter: drop-shadow(0 0 20px rgba(241, 91, 36, 0.1));
  }

  @keyframes breeze-float {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-15px) rotate(1deg);
    }
  }

  @keyframes soft-light {
    0% {
      filter: drop-shadow(0 0 20px rgba(241, 91, 36, 0.15)) brightness(1);
    }
    100% {
      filter: drop-shadow(0 0 40px rgba(241, 91, 36, 0.3)) brightness(1.05);
    }
  }

  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.5;
      transform: scale(1);
    }
    50% {
      opacity: 0.8;
      transform: scale(1.05);
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 6s ease-in-out infinite;
  }
</style>
