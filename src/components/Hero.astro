---
import Wrapper from "./Wrapper.astro";
import heroData from "../data/hero.json";
import siteData from "../data/site.json";

interface Props {
  locale: string;
}

const { locale } = Astro.props;
const content = heroData[locale as keyof typeof heroData];
const isEs = locale === "es";

const backgroundSymbols = [
  "/images/patterns/symbol1.svg",
  "/images/patterns/symbol2.svg",
  "/images/patterns/symbol1.svg",
  "/images/patterns/symbol2.svg",
];
---

<section
  class="relative min-h-dvh flex items-center justify-center bg-black bg-cover bg-center overflow-hidden pt-28 pb-12"
  style="background-image: url('/venue/3-ciudad.jpg');"
>
  <!-- Main Background Overlay (Darker) -->
  <div
    class="absolute inset-0 bg-linear-to-b from-black/90 via-black/70 to-black z-0 pointer-events-none"
  >
  </div>

  <!-- Content Contrast Overlay (Radial) -->
  <div
    class="absolute inset-0 bg-[radial-gradient(circle_at_center,_rgba(0,0,0,0.6)_0%,_transparent_10%)] z-0 pointer-events-none"
  >
  </div>

  <!-- Decorative Mesh Glows -->
  <div
    class="absolute -top-1/4 -left-1/4 w-[60vw] h-[60vw] bg-primary-500/10 rounded-full blur-[120px] pointer-events-none"
  >
  </div>
  <div
    class="absolute -bottom-1/4 -right-1/4 w-[50vw] h-[50vw] bg-primary-600/5 rounded-full blur-[100px] pointer-events-none"
  >
  </div>

  <Wrapper
    class="relative z-10 w-full h-full flex flex-col items-center justify-center text-center"
  >
    <div
      class="max-w-5xl mx-auto space-y-6 md:space-y-10 flex flex-col items-center hero-content-fade-in"
    >
      <!-- Brand Pill -->
      <div
        class="inline-flex items-center gap-2 p-1 px-4 rounded-full bg-black/40 border border-white/10 backdrop-blur-md shadow-2xl"
      >
        <span class="w-1.5 h-1.5 rounded-full bg-primary-500 animate-pulse"
        ></span>
        <span
          class="text-[0.6rem] md:text-[0.7rem] uppercase tracking-[0.25em] font-black text-white/90"
        >
          {content.event_type}
        </span>
      </div>

      <!-- Main Title -->
      <div class="relative group">
        <h1
          class="text-4xl sm:text-6xl md:text-7xl lg:text-[5rem] font-bold text-white leading-[0.9] tracking-tighter drop-shadow-[0_4px_20px_rgba(0,0,0,0.8)] font-heading"
        >
          KHIPU<span
            class="text-primary-500 inline-block transform hover:scale-110 transition-transform duration-500"
            >x</span
          >
          <span class="block mt-2 text-3xl sm:text-5xl md:text-6xl opacity-95"
            >Ilo</span
          >
        </h1>
        <div
          class="absolute -bottom-4 left-1/2 -translate-x-1/2 w-20 h-0.5 bg-linear-to-r from-transparent via-primary-500 to-transparent opacity-60"
        >
        </div>
      </div>

      <!-- Tagline / Description -->
      <p
        class="text-base md:text-lg text-white font-semibold max-w-2xl leading-relaxed px-4 drop-shadow-[0_2px_8px_rgba(0,0,0,0.8)]"
      >
        {content.description}
      </p>

      <!-- Event Details & Actions Row -->
      <div class="w-full space-y-6 md:space-y-8">
        <div class="flex flex-wrap justify-center gap-6 md:gap-10">
          <!-- Date -->
          <div class="flex items-center gap-3 group">
            <div
              class="p-2 rounded-xl bg-white/5 border border-white/10 group-hover:border-primary-500/30 transition-colors"
            >
              <svg
                class="w-4 h-4 text-primary-500"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                ></path>
              </svg>
            </div>
            <span class="text-xs md:text-sm font-semibold text-white/90"
              >{isEs ? "18-19 Agosto, 2026" : "Aug 18-19, 2026"}</span
            >
          </div>
          <!-- Location -->
          <div class="flex items-center gap-3 group">
            <div
              class="p-2 rounded-xl bg-white/5 border border-white/10 group-hover:border-primary-500/30 transition-colors"
            >
              <svg
                class="w-4 h-4 text-primary-500"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                ></path>
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
            </div>
            <span class="text-xs md:text-sm font-semibold text-white/90"
              >{isEs ? "Ilo, Perú" : "Ilo, Peru"}</span
            >
          </div>
        </div>

        <!-- Call to Actions -->
        <div
          class="flex flex-col sm:flex-row items-center justify-center gap-4 px-4"
        >
          <a
            href={isEs ? "/es/register" : "/en/register"}
            class="relative px-8 py-3 bg-primary-500 text-white rounded-full font-bold text-base md:text-lg transition-all duration-300 shadow-[0_10px_30px_rgba(241,91,36,0.3)] hover:shadow-[0_15px_40px_rgba(241,91,36,0.5)] hover:scale-105 active:scale-95 w-full sm:w-auto overflow-hidden group/btn"
          >
            <div
              class="absolute inset-0 -translate-x-full bg-linear-to-r from-transparent via-white/20 to-transparent group-hover/btn:translate-x-full transition-transform duration-700"
            >
            </div>
            <span class="relative z-10 flex items-center gap-2">
              {content.cta_primary}
              <svg
                class="w-5 h-5 transition-transform group-hover/btn:translate-x-1"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </span>
          </a>
          <a
            href={isEs ? "/es/schedule" : "/en/schedule"}
            class="px-8 py-3 bg-white/5 border border-white/10 backdrop-blur-md text-white rounded-full font-bold text-base md:text-lg transition-all duration-300 hover:bg-white/10 hover:border-white/20 hover:scale-105 w-full sm:w-auto"
          >
            {content.cta_secondary}
          </a>
        </div>
      </div>

      <!-- Stats Floating Cards -->
      <div class="pt-4 w-full">
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 md:gap-6">
          {
            content.stats.map(
              (stat: { value: string; label: string }, index: number) => (
                <div
                  class="stat-card flex flex-col items-center justify-center p-4 md:p-6 rounded-2xl bg-white/5 border border-white/10 backdrop-blur-xl shadow-2xl transition-all duration-500 hover:bg-white/10 hover:-translate-y-1 group"
                  style={`animation-delay: ${index * 0.1}s`}
                >
                  <div class="text-2xl md:text-3xl font-black text-white mb-0.5 group-hover:text-primary-400 transition-colors font-heading tracking-tight">
                    {stat.value}
                  </div>
                  <div class="text-[0.6rem] md:text-[0.65rem] text-white/50 uppercase tracking-[0.2em] font-bold group-hover:text-white/80 transition-colors">
                    {stat.label}
                  </div>
                </div>
              ),
            )
          }
        </div>
      </div>
    </div>
  </Wrapper>
</section>

<style>
  .hero-content-fade-in {
    opacity: 0;
    transform: translateY(30px);
    animation: hero-appear 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes hero-appear {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .stat-card {
    opacity: 0;
    transform: translateY(20px);
    animation: card-appear 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes card-appear {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
